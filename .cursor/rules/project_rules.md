# 后端项目规则引用

> **📌 本文档是规则引用文件**  
> 本目录下的所有代码必须严格遵循以下规则文档：

## 规则文档层级

1. **公共规则**（必须遵循）：
   - `../../.rules/project_rules.md` - 所有项目通用规范

2. **后端特定规则**（必须遵循）：
   - `../../PROJECT_RULES.md` - 后端项目特定规范

## 阅读顺序

1. 首先阅读公共规则：`../../.rules/project_rules.md`
2. 然后阅读后端规则：`../../PROJECT_RULES.md`

## 核心要求

- ✅ 所有代码必须符合公共规则
- ✅ 所有代码必须符合后端特定规则
- ✅ 必须使用模块别名（`@database`、`@widget`、`@router`、`@server`、`@config`）
- ✅ 必须使用参数化查询，防止 SQL 注入
- ✅ 统一使用 `@widget/constant/code` 错误码
- ✅ 所有文件必须包含 `@copyright` 标签

---

**请严格遵循上述规则文档进行代码开发**
